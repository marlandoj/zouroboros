export interface Config{model?:string|null;timeoutMs?:number|null;retries?:number|null} const keys=new Set(["model","timeoutMs","retries"]); export function resolveConfig(...layers:Config[]):Config{const out:Config={};for(const layer of layers){for(const [key,value] of Object.entries(layer)){if(!keys.has(key))throw new Error("unknown config key: "+key);if(value!==undefined)(out as Record<string,unknown>)[key]=structuredClone(value)}}return out}
